def execute(String workspace) {
    stage ("Build") {
        echo "Build"
    }

    stage ("Test") {
       echo "Test"
    }
    
    stage("Hello"){
       sh "${workspace}/hello.sh"
    }
}

return this
